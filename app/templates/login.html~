<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{ title }}</title>
</head>
<body>
  <h1>{{ subtitle }}</h1>
  <form method="post">
    <p><input type=text name=username>
    <p><input type=password name=password>
    <p><input type=submit value=Login>
  </form>
  <nav>
    <ul>
      {% for href, id, caption in navlist %}
      <li id ="{{ id|e }}" href="{{ href|e }}">{{ caption|e }} </li>
      {% endfor %}
    </ul>
  </nav>
  <p>
    Some content
  </p>
</body>
</html>


